package com.example.collection.list;

/**
 * Vector
 * 1.线程安全的 ArrayList（所有方法加上 synchronized锁 修饰）
 * 2.•ArrayList在底层数组不够用时在原来的基础上扩展0.5倍，Vector是扩展1倍
 * 3.List list = Collections.synchronizedList(new ArrayList(...));就可以实现同步
 */
public class VectorTest {
}
